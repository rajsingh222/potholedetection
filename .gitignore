# Ignore the massive training image datasets (too large)
# But KEEP Data/test/ for users to evaluate model
Data/train/
Data/test/annotations/
dataset/

# Ignore local training run outputs/logs
runs/
# Keep only sample results, ignore uploads
static/uploads/
static/results/

# Test images and evaluation results - KEEP for users
# sample_test/ - included
# test_results/ - included

# Prevent uploading the huge pre-trained base model weights
yolo11n.pt

# KEEP your custom trained weights file (best.pt) if it is under 100MB
# If best.pt is larger than 100MB, uncomment the line below:
# best.pt